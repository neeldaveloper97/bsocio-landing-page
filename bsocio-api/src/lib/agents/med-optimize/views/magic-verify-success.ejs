<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Signed In</title>
    <style>
      body {
        font-family: Inter, Roboto, Arial, sans-serif;
        background: #f6f8fb;
        color: #111827;
      }
      .card {
        max-width: 680px;
        margin: 60px auto;
        padding: 28px;
        background: #fff;
        border-radius: 8px;
        box-shadow: 0 6px 24px rgba(15, 23, 42, 0.06);
      }
      .btn {
        display: inline-block;
        padding: 12px 20px;
        background: #2563eb;
        color: #fff;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 600;
      }
      .muted {
        color: #6b7280;
        font-size: 13px;
      }
    </style>
    <script>
      // Auto-redirect after delay
      (function(){
        const delay = <%= redirectDelay %> || 3000;
        setTimeout(function(){
          try { window.location.href = '<%= frontend %>'; } catch(e){}
        }, delay);
      })();
    </script>
  </head>
  <body>
    <div class="card">
      <h2>You're signed in</h2>
      <p class="muted">Hello <%= email %>,</p>
      <p>
        You've been successfully signed in. We'll redirect you to the app
        shortly.
      </p>
      <p style="margin-top: 18px">
        <a class="btn" href="<%= frontend %>">Continue to app</a>
      </p>
      <p class="muted" style="margin-top: 18px">
        If nothing happens, click the button above.
      </p>
    </div>
  </body>
</html>
